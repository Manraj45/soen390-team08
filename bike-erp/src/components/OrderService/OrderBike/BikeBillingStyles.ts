import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
  billingBox: {
    margin: 20
  },
  item: {
    float:"left",
    left:0,
    color: "red",
  }
}));

export default useStyles;