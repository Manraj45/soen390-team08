import { makeStyles } from "@material-ui/core";

const useStyles = makeStyles((theme) => ({
    title: {
      textAlign: "center",
      fontWeight: "bold",
      fontFamily: "Inter",
      fontSize: "64",
    },
    background: {
      background: "#DDDDDD",
    },
  }));

export default useStyles;